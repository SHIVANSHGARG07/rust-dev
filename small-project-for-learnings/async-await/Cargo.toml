[package]
name = "async-await"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.13", features = ["json"] }
error-chain = "0.12"
tokio = { version = "1", features = ["full"] }
