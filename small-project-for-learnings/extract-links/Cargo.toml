[package]
name = "extract-links"
version = "0.1.0"
edition = "2024"

[dependencies]
error-chain = "0.12.4"
reqwest = "0.11.12"

# as we have links in form of html and a tags 
select = "0.5.0"

tokio = { version = "1.21.1", features = ["full"] }